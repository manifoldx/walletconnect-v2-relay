import * as rpcJsonUtils from "rpc-json-utils";

export const isServerErrorCode = rpcJsonUtils.isServerErrorCode;

export const isReservedErrorCode = rpcJsonUtils.isReservedErrorCode;

export const getError = rpcJsonUtils.getError;

export const getErrorByCode = rpcJsonUtils.getErrorByCode;
